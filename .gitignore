.env
new_token.txt
__pycache__/
*.pyc
.venv/
venv/
# Generated slides are disposable and ignored locally to avoid merge conflicts.
# CI force-adds them (git add -f output/) so they're still served publicly for
# the Graph API at the run's commit.
output/

# Cached stock photos (re-downloadable)
assets/cache/
